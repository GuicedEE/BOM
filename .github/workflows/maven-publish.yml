name: Guiced EE (BOM)
on:
  workflow_dispatch:
    inputs:
      centralRelease:
        type: boolean
        description: Run a release to maven central
        default: false
  push:
env:
  GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
  GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}

permissions:
  contents: read
  packages: write

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      versioner: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.versioner }}
      standalone: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.standalone }}
      tests: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.tests }}
      swagger: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.swagger }}
      jboss: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.jboss }}
      jakarta: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.jakarta }}
      hibernate: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.hibernate }}
      google: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.google }}
      fasterxml: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.fasterxml }}
      apachecxf: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.apachecxf }}
      apachecommons: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.apachecommons }}
      smallrye: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.smallrye }}
      guicedee: ${{ github.event_name == 'workflow_dispatch' && 'true' || steps.filter.outputs.guicedee }}
    steps:
      - uses: actions/checkout@v6.0.2
        if: github.event_name == 'push'
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36 # v3
        if: github.event_name == 'push'
        id: filter
        with:
          filters: |
            versioner:
              - 'Versioner/pom.xml'
            standalone:
              - 'StandaloneBOM/pom.xml'
            tests:
              - 'TestLayoutBOM/pom.xml'
            swagger:
              - 'SwaggerBOM/pom.xml'
            jboss:
              - 'JBossBOM/pom.xml'
            jakarta:
              - 'JakartaBOM/pom.xml'
            hibernate:
              - 'HibernateBOM/pom.xml'
            google:
              - 'GoogleBOM/pom.xml'
            fasterxml:
              - 'FasterXMLBOM/pom.xml'
            apachecxf:
              - 'ApacheCXFBOM/pom.xml'
            apachecommons:
              - 'ApacheBOM/pom.xml'
            smallrye:
              - 'SmallRyeBOM/pom.xml'
            guicedee:
              - 'pom.xml'
  # Layer 1: Versioner (root - no BOM dependencies)
  Versioner:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.versioner == 'true' }}
    needs:
      - detect-changes
    with:
      baseDir: 'Versioner/'
      name: 'Versioner'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit

  # Layer 2: Standalone (depends on Versioner only)
  Standalone:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.standalone == 'true' }}
    needs:
      - detect-changes
      - Versioner
    with:
      baseDir: 'StandaloneBOM/'
      name: 'Standalone'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit

  # Layer 3: BOMs that depend on Standalone
  Jakarta:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.jakarta == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'JakartaBOM/'
      name: 'Jakarta'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  Apache-Commons:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.apachecommons == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'ApacheBOM/'
      name: 'Apache'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  Apache-CXF:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.apachecxf == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'ApacheCXFBOM/'
      name: 'Apache CXF'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  Google:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.google == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'GoogleBOM/'
      name: 'Google'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  JBoss:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.jboss == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'JBossBOM/'
      name: 'JBoss'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  Hibernate:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.hibernate == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'HibernateBOM/'
      name: 'Hibernate'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  Swagger:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.swagger == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'SwaggerBOM/'
      name: 'Swagger'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  SmallRye:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.smallrye == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'SmallRyeBOM/'
      name: 'SmallRye'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
  Tests:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.tests == 'true' }}
    needs:
      - detect-changes
      - Standalone
    with:
      baseDir: 'TestLayoutBOM/'
      name: 'Test Layouts'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit

  # Layer 4: FasterXML (depends on Standalone + Jakarta)
  FasterXML:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.fasterxml == 'true' }}
    needs:
      - detect-changes
      - Standalone
      - Jakarta
    with:
      baseDir: 'FasterXMLBOM/'
      name: 'FasterXML'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit

  # Layer 5: GuicedEE root BOM (depends on Apache, FasterXML, Standalone, Jakarta, Google, Tests)
  GuicedEE:
    uses: GuicedEE/Workflows/.github/workflows/bom.yml@master
    if: ${{ !cancelled() && needs.detect-changes.outputs.guicedee == 'true' }}
    needs:
      - detect-changes
      - Standalone
      - Jakarta
      - Apache-Commons
      - FasterXML
      - Google
      - Tests
    with:
      baseDir: ''
      name: 'Guiced-EE BOM'
      publishToCentral: ${{inputs.centralRelease}}
    secrets: inherit
